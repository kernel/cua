{
  "name": "@onkernel/cua-agent",
  "version": "0.1.0",
  "description": "Kernel browser computer-use AgentTools and agent factory built on pi-agent-core",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest --run"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.74.0",
    "@earendil-works/pi-ai": "^0.74.0",
    "@onkernel/cua-ai": "0.1.0",
    "@onkernel/sdk": "0.49.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
