{
  "name": "cua",
  "version": "0.1.0",
  "description": "Kernel-cloud-browser computer-use: CLI and SDKs",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/ai",
    "packages/agent",
    "packages/cua-translator",
    "packages/cua-openai",
    "packages/cua-anthropic",
    "packages/cua-gemini",
    "packages/cua-tzafon",
    "packages/cua-yutori",
    "packages/ptywright",
    "packages/cua-cli"
  ],
  "scripts": {
    "build": "tsc -b && node ./scripts/append-js-extensions.mjs && npm run build:native --workspace @onkernel/ptywright --if-present",
    "build:cli": "npm run build --workspace @onkernel/cua-cli",
    "dev": "tsc -b --watch",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean && npm run clean:native --workspace @onkernel/ptywright --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "22.18.4",
    "tsx": "^4.21.0",
    "typescript": "5.9.3"
  }
}
